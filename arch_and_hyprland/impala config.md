# Impala Configuration

## Overview
Music player daemon (MPD) client.

## Configuration
- **`~/.config/impala/config.toml`** — Only one option set.
- `esc_quit = true` — Escape key quits the application.

## Notes
- Everything else uses impala's defaults.
- Requires an MPD instance running.

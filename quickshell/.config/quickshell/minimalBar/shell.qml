import Quickshell

// Placeholder entry point. Importing a folder here makes quickshell generate
// its module (qmldir) so the QML language server can resolve types like
// `Globals` while you build out the templates. Add more `import qs.<folder>`
// lines as you create them, then reload (./reload.sh) + :LspRestart in nvim.
import qs.templates

ShellRoot {
}
